namespace Gotcha2.API.Services.Helpers.Extensions
{
    // Central entity → DTO mapping.
    // One method per direction; controllers call these via the `using Gotcha2.API.Services.Helpers.Extensions;` import.
    // Methods are added per-resource as §2 (Players), §3 (Games), §4 (Kills), §5 (TargetAssignments), §6 (Users) progress.
    public static class MappingExtensions
    {
    }
}
